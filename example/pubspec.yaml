name: example
description: an example showcase for jaspr_svg
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  jaspr: ^0.23.0
  jaspr_icons_pack:
    path: ../

dev_dependencies:
  build_runner: ^2.6.0
  build_web_compilers: ^4.2.4
  jaspr_builder: ^0.23.0
  jaspr_lints: ^0.7.0
  lints: ^5.0.0

jaspr:
  mode: static
